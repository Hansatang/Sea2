package mseProg2;

public class SquareAndMultiply {
    public int squareAndMultiply(int base, int exponent, int modulo){

        // The following code puts the exponent into an array of size 32.
        // If the exponent is 23 then the array will be: [0,0,...,0, 1, 0, 1, 1, 1]
        // There are more efficient ways in java to work with binary numbers but that is more complex
        // and you are welcome to use this code snippet if you like.

        /*
        int exponentAsArr[] = new int[32];
        for(int i=31; i>=0; i--){
            exponentAsArr[i]=exponent%2;
            exponent = exponent/2;
        }
        */

        // TODO implement method

        return 0;
    }



}
